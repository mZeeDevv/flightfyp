<div className="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 className="text-2xl font-bold mb-6 text-center">Find Your Flight</h1>
        <div className="space-y-4">
          <div>
            <label className="block text-sm font-medium text-gray-700">From</label>
            <input
              type="text"
              placeholder="Enter departure city"
              value={from}
              onChange={(e) => setFrom(e.target.value)}
              className="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <div>
            <label className="block text-sm font-medium text-gray-700">To</label>
            <input
              type="text"
              placeholder="Enter destination city"
              value={to}
              onChange={(e) => setTo(e.target.value)}
              className="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <div>
            <label className="block text-sm font-medium text-gray-700">Departure Date</label>
            <input
              type="date"
              value={departureDate}
              onChange={(e) => setDepartureDate(e.target.value)}
              className="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <button
            onClick={handleSearch}
            className="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
            Search Flights
          </button>

          {/* Loading State */}
          {loading && <p className="text-blue-500 mt-4 text-center">Loading flights...</p>}

          {/* Error State */}
          {error && <p className="text-red-500 mt-4 text-center">{error}</p>}

          {/* Display Flights */}
          {flights.length > 0 && (
            <div className="mt-4">
              <h2 className="text-lg font-bold mb-2">Available Flights:</h2>
              <ul className="space-y-4">
                {flights.map((flight, index) => (
                  <li key={index} className="p-4 bg-gray-100 rounded-lg shadow-md">
                    <p className="text-sm text-gray-700">
                      <strong>Airline:</strong> {flight.validatingAirline?.name}
                    </p>
                    <p className="text-sm text-gray-700">
                      <strong>Price:</strong> {flight.fare.total.amount} {flight.fare.total.currency}
                    </p>
                    <p className="text-sm text-gray-700">
                      <strong>Duration:</strong> {flight.journeyDuration}
                    </p>
                  </li>
                ))}
              </ul>
            </div>
          )}
        </div>
      </div>